import Link from 'next/link'
import { ArrowLeft, Check, Lock, Server } from 'lucide-react'
import { Shell } from '@/components/site-shell'

const promises = ['Your text is analyzed in your browser.', 'Nothing is sent to a server or used to train a model.', 'History is stored only in your browser’s local storage.', 'You can delete your saved analyses at any time.']
export default function PrivacyPage() { return <Shell><main className="mx-auto max-w-3xl px-5 py-12 sm:py-20"><Link href="/" className="mb-14 flex w-fit items-center gap-2 font-mono text-xs text-muted-foreground hover:text-accent"><ArrowLeft size={14} /> BACK HOME</Link><p className="font-mono text-xs uppercase tracking-widest text-accent">The quiet promise</p><h1 className="mt-4 text-4xl font-semibold sm:text-5xl">Your words stay yours.</h1><p className="mt-6 text-base leading-7 text-muted-foreground">GhostFilter was designed around a simple constraint: your text should not need to leave your device to become more legible.</p><div className="mt-12 grid gap-3">{promises.map((promise) => <div key={promise} className="flex items-start gap-4 rounded-md border border-border bg-card p-5"><span className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-accent text-accent-foreground"><Check size={14} /></span><p className="text-sm leading-6">{promise}</p></div>)}</div><div className="mt-12 grid gap-4 sm:grid-cols-2"><div className="border border-border p-6"><Lock size={20} className="text-accent" /><h2 className="mt-6 font-semibold">No account required</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">There is no profile to build and no identity attached to your checks.</p></div><div className="border border-border p-6"><Server size={20} className="text-accent" /><h2 className="mt-6 font-semibold">No server storage</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">The analyzer runs locally. Inspect or clear browser storage whenever you want.</p></div></div><p className="mt-12 border-t border-border pt-6 text-xs leading-5 text-muted-foreground">GhostFilter is an educational tool for noticing language patterns. It does not determine intent, truth, or whether a message is manipulative.</p></main></Shell> }
